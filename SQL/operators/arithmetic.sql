-- use this operator to perform arithmetic calculation like +,-,*,/

select stud_id+100 from student;
-- stud_id is column name while student is table
-- it will give stud_id values with addition of 100 in it

select stud_id-100 from student;
-- it will give stud_id values with substraction of 100 in it

select stud_id*100 from student;
-- it will give stud_id values with multiplication of 100 in it

select stud_id/100 from student;
-- it will give stud_id values with division of 100 in it